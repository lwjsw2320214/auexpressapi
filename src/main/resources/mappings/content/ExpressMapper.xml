<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        <mapper namespace="com.auexpress.dao.content.ExpressMapper">

    <select id="getList" resultType="Express">
        SELECT * FROM [ems_kind] WHERE ninput=1 ORDER BY idocweighte
    </select>
    
</mapper>